apiVersion: v1
kind: Service

metadata:
  name: employee-app
  namespace: employee

spec:
  type: ClusterIP

  selector:
    app: employee-app

  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
